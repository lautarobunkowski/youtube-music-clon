import menu from "@icons/menu.svg";

const Layout = () => {
  return (
    <div>
        <nav>
            <img src={menu} alt="menu" className="text-white"/>
        </nav>
    </div>
  )
}

export default Layout